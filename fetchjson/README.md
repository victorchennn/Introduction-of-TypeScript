# Fetchjson

In the project directory, you can run:

```bash
$ tsc index.ts
$ node index.js
// or
$ ts-node index.ts
```

For the project, you should install:

```bash
$ npm install ts-node axios
```
